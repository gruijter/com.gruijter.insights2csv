"use strict";

var mergeObjects = require("merge");

function merge() {
    for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
        args[_key] = arguments[_key];
    }

    return mergeObjects.recursive.apply(mergeObjects, [true].concat(args));
}

module.exports = {
    merge
};