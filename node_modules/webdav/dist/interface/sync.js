"use strict";

function syncDirectories(local, remote, opts = {}) {
    var _opts$update = opts.update;
    const update = _opts$update === undefined ? "remote" : _opts$update;

    if (/^(remote|local|both)$/.test(update) !== true) {
        throw new Error(`Invalid update target: ${update}`);
    }
}